package access_specifiers.default_example.another_example_package;

import access_specifiers.default_example.example_package.*;

public class Example {

    static void main() {
        //DefaultClass obj = new DefaultClass(); // this won't work since for default access is at package-level, and we are in different package

    }
}
